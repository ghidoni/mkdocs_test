name: Linter
on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - main

jobs:
  CI:
    name: CI Pipeline
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Linter
        uses: github/super-linter@v4
